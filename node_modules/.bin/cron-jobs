#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cron-jobs/bin/cron-jobs" "$@"
  ret=$?
else 
  node  "$basedir/../cron-jobs/bin/cron-jobs" "$@"
  ret=$?
fi
exit $ret
